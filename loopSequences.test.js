
// npm init -y                   //installs package json
// npm install --save-dev jest   //testing library for development
// test script = "jest"          //type "npm test" to run tests

const { squared_sum, mix} = require('./loopSequences')